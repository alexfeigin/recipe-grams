// Google Search Console verifies the /recipe-grams/ URL-prefix property at this exact path.
// Keep the filename and response body in sync with the downloaded verification file.
export function GET() {
  return new Response("google-site-verification: google897e637a154db3cd.html");
}
