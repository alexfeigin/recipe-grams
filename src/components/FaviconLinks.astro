---
import { sitePath } from "../lib/recipePages";

const basePath = import.meta.env.BASE_URL;
---

<link rel="icon" href={sitePath(basePath, "favicon.ico")} sizes="any" />
<meta name="theme-color" content="#b43e2f" />
